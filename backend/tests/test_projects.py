"""
Integration tests for /api/projects endpoints.
Requires a running database — configure FGA_FORGE_DATABASE_URL in .env.
"""
import pytest


# TODO: add tests using httpx AsyncClient + pytest-asyncio
